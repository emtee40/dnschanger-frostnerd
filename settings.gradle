import groovy.io.FileType
def list = []

def dir = rootProject.projectDir
dir.eachFileRecurse (FileType.FILES) { file ->
    list << file
}

include ':app'
include ':library'
project(':library').projectDir = new File(rootProject.projectDir,'AndroidUtils\\library')
